# Test fixture: presence of this filename triggers the Shai-Hulud incident
# file_artifact match. Contents are intentionally a no-op; the IOC is the
# filename itself.
name: shai-hulud
on: push
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
